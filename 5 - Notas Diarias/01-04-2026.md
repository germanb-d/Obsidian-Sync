Deuda tecnica: Toda aquellos metodos u funciones erroneas se van amontonando en una "deuda", por lo cual aunque las puedo realizar, despues poco a poco debo corregirlas ("ir pagando la deuda")

Si en algun caso se da que un metodo puede devolver null: 
Usar optional > usar exception > devolver null


Rapear una excepcion?

Puedo recibir un tipo optional con un var. Es bueno para los lambda
Para una list por ejemplo no es necesario porque devuelve la lista vacia, ya si es un objeto que devuelva optional o exception, nunca null

IsPresent como booleano es como preguntar por NULL?

El @override es para q te marque si escribiste mal el metodo, aunque funcione igual sin el


Diferencia entre introducir variable o constant. Si tengo multiplicar por 0.1, cual me conviene usar apra refactorizar?



Primero hago los test y luego refactirizo para evitar romper algo.

Nos dan un codigo y debemos implementar tell dont aks, etc.